#pragma once

enum Syscalls
{
    ALLOCATE,
    ALLOCATE_ALIGNED,
    UNALLOCATE,

    CHECK_FOLDER,
    CHECK_FILE,
    CREATE_FOLDER,
    CREATE_FILE,
    GET_FILES,
    GET_FILE,
    DELETE_FILE,

    GET_FEMTOSECONDS,

    CREATE_THREAD,
    WAIT_FOR_THREAD,
    DESTROY_THREAD,
    EXIT_THREAD,

    REGISTER_KEYBOARD,
    UNREGISTER_KEYBOARD,

    REGISTER_MOUSE,
    UNREGISTER_MOUSE,

    OBTAIN_DISPLAY,
    RELEASE_DISPLAY,

    REGISTER_TTY,
    UNREGISTER_TTY,
    PUT,
    WRITE,
    READ,

    EXECUTE,
    QUIT,

    REGISTER_LISTENER,
    UNREGISTER_LISTENER,
    SEND_MESSAGE
};
